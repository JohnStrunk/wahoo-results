"""Parses CTS Start List files (*.scb) from Hy-Tek Meet Manager."""

def dummy(val: int) -> int:
    """
    This is here temporarily to test CI.

    >>> dummy(1)
    2
    >>> dummy(8)
    9
    """
    return val + 1
