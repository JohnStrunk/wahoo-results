{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Serve MkDocs",
            "type": "shell",
            "command": "pipenv run mkdocs serve",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
